## Packages
framer-motion | Complex animations for page transitions and card interactions
lucide-react | Beautiful icons (already in base, but emphasizing usage)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
Authentication uses Replit Auth (useAuth hook).
Images will use Unsplash placeholders for now.
South African aesthetic: Warm, vibrant, earthy colors.
